# PUBLIC_INTERFACE
# React app expects the backend base URL (Flask API) to be provided here.
# Orchestrator should set this in the .env file for the frontend container.
REACT_APP_API_BASE_URL=http://localhost:5000
